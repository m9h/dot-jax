---
name: Use uv for Python development
description: User prefers uv over pip for all Python package management
type: feedback
---

Use `uv` for all Python package management (install, pip, etc.), not `pip` directly.

**Why:** User preference for their Python workflow tooling.

**How to apply:** Replace `pip install` with `uv pip install`, etc. in all development commands.
